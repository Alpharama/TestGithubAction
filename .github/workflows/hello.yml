name: Hello World

on:
  schedule:
    - cron: '*/5 * * * *'  # toutes les 5 minutes

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, world!"

